# HeliNav — VFR ヘリコプター ナビゲーション (PWA)

ヘリコプター操縦士向けの **無料・軽量・オフライン対応** の VFR ナビゲーション
ウェブアプリ (PWA) です。iPad / iPhone をコックピットに設置しての使用を想定し、
**大きな表示・少ないタップ数** で操作できるよう設計しています。

> ⚠️ **重要（免責）**: 本アプリは**参考情報**を提供するものです。収録データには
> 概略・未検証のものを含みます。飛行前ブリーフィング・航空図・**AIP Japan**・
> NOTAM 等の**公式情報で必ず確認**し、最終的な運航判断はパイロットが行って
> ください。運航用の航法計器・認証された EFB の代替にはなりません。

## 要件との対応

Notion 要件定義（ヘリコプター操縦士向けナビゲーションアプリ）に対応:

| 要件 | 実装 |
|---|---|
| VFR メイン（IFR は将来） | VFR 前提の地図・ルート機能 |
| 無料で軽量な動作 | 依存は Leaflet のみ・ビルド不要の静的サイト |
| Web ブラウザ / PWA | `manifest` + Service Worker でホーム画面追加・スタンドアロン起動 |
| オフライン動作（必須） | アプリ本体・データ・閲覧タイルをキャッシュ、エリア一括保存 |
| 無料 API・オープンデータ | OpenStreetMap タイル + OSM/公開データの GeoJSON |
| 飛行場・VOR 表示 | `airports` / `navaids` レイヤー |
| ルート・ウェイポイント・フィックス | タップでルート作成、区間ごとの磁方位・距離・ETA |
| ドクターヘリ基地病院 | `heliports` レイヤー（病院＋公共ヘリポート） |
| 送電線情報 | `hazards` レイヤー（サンプル＋OSM から範囲取得） |
| iPad/iPhone・大画面・少タップ | 大きなタッチ FAB / レイヤーボタン、昼夜切替 |

## 使い方（起動）

ビルド不要です。静的ファイルを配信するだけで動きます。

```bash
cd webapp
python3 -m http.server 8000
# ブラウザで http://localhost:8000/ を開く
```

Service Worker（オフライン機能）は `https://` または `localhost` でのみ有効です。
iPad/iPhone では Safari で開き、**共有 → ホーム画面に追加** でフルスクリーンの
アプリとして起動できます。

## 主な機能

Garmin Pilot / Air Navigation Pro を参考に、**飛行中にグローブ・振動環境でも
一目で読める** ことを最優先に設計しています。

### 飛行中 (インフライト)

- **NAVバー（画面上部の常時表示計器）**: GS(kt)・TRK(磁トラック)・ALT(GPS高度ft)、
  アクティブWPへの **BRG / DIST / ETE / XTK(コース横偏位 L/R)** を大型数字で表示。
  GPS 起動は自動。GPS の course が取れない端末でも位置差分からトラックを自己計算。
- **トラックアップ**: `N↑ / TRK↑` ボタンで切替。進行方向が常に画面上、
  自機は画面下 1/3 に置き**前方を広く表示**（マーカー・ラベルは常に正立）。
- **アクティブレグ＋自動シーケンス**: 現在のレグを**マゼンタ**で強調。
  WP の 0.3NM 以内通過または通り過ぎで**自動的に次のレグへ進む**。
  ルート一覧の行タップで任意のレグをアクティブ化。
- **D→ ダイレクト**: 識別コード(RJTT)・名称(羽田)・所在地で全施設を検索し、
  ワンタップで現在地からのダイレクトを設定。
- **NRST（最寄り）**: 現在地から近い順に空港 / ヘリポート・病院を距離・方位付きで
  一覧表示。緊急時・プラン変更時にワンタップでダイレクト。
- **Wake Lock**: 飛行中に画面がスリープしない（対応ブラウザ）。
- **ズーム連動デクラッタ**: 広域ではマーカーを縮小・ヘリポートは病院を優先表示、
  ズームすると識別コードラベル(ICAO/VOR)を表示。
- **デモ飛行**: メニューから、作成したルートを計画速度で模擬走行して
  NAVバー・シーケンス・トラックアップを地上で確認できる。

### 気象・障害物・計画 (Garmin Pilot 参考のデータ機能)

- **METAR/TAF ライブ気象**: [aviationweather.gov](https://aviationweather.gov) の
  無料 API から日本全域の METAR/TAF を一括取得（10 分ごと自動更新・メニューから
  手動更新も可）。空港シンボル右上に**フライトカテゴリ**
  (VFR 緑 / MVFR 青 / IFR 赤 / LIFR 紫) のバッジを表示し、空港シートで
  **解読値（風・視程・雲・気温/露点・QNH）と生電文 (METAR/TAF)** を確認可能。
  最終取得はローカル保存され、オフライン時も参照可（70 分超は「古い」表示）。
- **障害物**: 東京スカイツリー等の著名高層構造物を収録。さらにメニュー →
  「送電線・障害物を取得」で表示範囲の OSM から鉄塔・煙突・通信塔・風車
  （高さ 50m 以上）を取得。航空図式の**三角シンボル＋高さ(ft)ラベル**で表示、
  タップで詳細（高さ・現在地からの距離/方位）。
- **日の出/日の入**: 施設シートに当日の日出/日没 (JST) を表示（VFR 日中制限の確認用）。
- **燃料計画**: メニューで燃料消費(L/h)・搭載燃料(L)を設定すると、ルートに
  **必要燃料＋30 分予備**を表示し、搭載燃料に対する不足を警告。
- **滑走路ミニ図**: 空港シートに滑走路方位のダイアグラムを表示。

### 計画・データ

- **地図**: OpenStreetMap。昼/夜モード（夜はタイルを減光し暗順応を保持。
  地図シンボルの色も夜用に自動調整）。
- **航空図スタイルのシンボル**: 空港=丸に滑走路(管制=青/非管制=マゼンタ)、
  ヘリポート=H、病院=赤H、VOR=六角形、NDB=点線円。ズームに応じて縮小・
  識別ラベル表示を自動切替。見た目は小さくてもタップ領域は 40px 確保。
- **空域レイヤー**: 管制圏 / 情報圏 / 飛行回避(原子力等) / 進入管制区(概略)。
  レイヤーパネル(右下 ▤)でクラスごとに表示切替でき、設定は保存されます。
  **重要空域(管制圏・情報圏・飛行回避)は既定で表示**。空域をタップすると
  名称・半径・高度・周波数を表示。※円による概略表示。要 AIP 照合。
- **レイヤー切替**（右下 ▤ パネル）: 空港 / ヘリ・病院 / VOR・NDB / 送電線 /
  ルート / 空域。
- **施設情報**: マーカーをタップ → 識別・標高・滑走路長・周波数・座標、現在地から
  の距離・磁/真方位を表示。「D→ ダイレクト」「ルートに追加」。
- **ルート作成**: `✚` でルートモード → 地図タップでウェイポイント追加。
  区間ごとに **磁方位・距離(NM)・ETE**、全長を表示。WP はドラッグで移動、
  `×` で削除。ETE は **GPS 実測 GS(20kt以上)を優先**、無ければ計画速度で計算。
- **オフライン保存**: メニュー →「表示エリアをオフライン保存」で現在表示の
  地図タイルを端末にキャッシュ。
- **ハザード取得**: メニュー →「表示範囲のハザード取得」で OSM の送電線
  (`power=line`) を取得しキャッシュ（オンライン時）。
- **データ取込**: メニュー →「GeoJSON を取込」で公式 AIP 等のデータに差し替え。

## データについて

`data/*.geojson` に収録。座標は WGS84。**運用前に必ず公式情報で照合**してください。

空港・ヘリポート・航法無線施設は **[OurAirports](https://ourairports.org/)
（パブリックドメイン）** のデータを基にしています。座標・標高・滑走路・
通信周波数・VOR 周波数は同データに準拠します。

| ファイル | 件数 | 内容 | 出典・精度 |
|---|---:|---|---|
| `airports.geojson` | 286 | 空港・飛行場（ICAO/IATA・種別・標高・滑走路・**TWR/ATIS/GND 等の実通信周波数**・所在） | OurAirports（実測ベース） |
| `heliports.geojson` | 3,036 | ヘリポート（うち **病院ヘリパッド 459**）・東京ヘリポート等 | OurAirports（実測ベース） |
| `navaids.geojson` | 219 | **VOR/VORTAC/VOR-DME/TACAN/NDB**（識別・**周波数**・CH・関連空港） | OurAirports（実測ベース） |
| `powerlines.geojson` | 2 | 送電線ハザード | サンプルのみ（[ハザード取得]で OSM から拡充） |
| `airspace.geojson` | 113 | 空域: 管制圏 63 / 情報圏 22 / 進入管制区 10 / 飛行回避(原子力) 18 | `scripts/generate_airspace.py` で空港周波数から概略導出（**円近似・要 AIP 照合**） |

VOR 系の周波数は MHz、NDB は kHz で表記します（例: 羽田 HME VOR-DME 112.20 MHz）。
公式データをお持ちの場合は [データ取込] から任意の GeoJSON で各レイヤーを
上書きできます（端末の localStorage に保存され次回以降も適用）。

> ℹ️ ヘリポートは 3,000 件超あるため、ズーム 8 未満では非表示、表示範囲内で最大
> 400 件（病院ヘリパッド優先）に間引いて描画します（コックピットでの視認性・軽量化）。

### GeoJSON フォーマット

各 Feature の `properties.type` で種別を判定します（`airport` / `heliport` /
`hospital` / `navaid` / `powerline`）。任意の追加項目:

```jsonc
{ "type": "Feature",
  "properties": {
    "type": "airport", "ident": "RJTT", "iata": "HND",
    "name": "東京国際空港", "elev_ft": 35, "rwy_ft": 12000, "twr": true },
  "geometry": { "type": "Point", "coordinates": [139.7811, 35.5533] } }
```

`meta.dataset`（`airports`/`heliports`/`navaids`/`hazards`）を付けておくと、
取込時に対象レイヤーを自動判定します。

## 既知の制約

- OSM タイル・Overpass API は**公共サービス**です。過度な取得は避け、
  利用ポリシーを尊重してください（本アプリはタイルを 1 回だけ保存）。
- 磁方位は設定した固定磁気偏差（既定 8°W）で近似します。地域差・経年変化は
  反映しません。
- **トラックアップ中は地図のドラッグ/ピンチを無効化**しています（回転座標系での
  誤動作防止）。ズームは左下の大ボタン、パンはノースアップに切替えて行って
  ください。
- GPS 高度は気圧高度ではなく **GPS 楕円体/ジオイド高**で、端末により取得できない
  場合があります。高度計の代替にはなりません。
- 送電線・障害物レイヤーは網羅的ではありません。低高度飛行の障害物確認は
  必ず航空図・現地情報で行ってください。
- **空域は円による概略表示**です。管制圏・情報圏は「TWR/RDO 周波数を持つ空港の
  半径 5NM」という近似で自動生成しており、実際の告示範囲・高度とは異なります。
  訓練/試験空域・制限区域(P/R/D)の正式な範囲は未収録です。公式データをお持ちの
  場合は `properties.type="airspace"` の GeoJSON を [データ取込] で差し替え可能。
- 地形は本バージョン未対応（今後の拡張候補）。

## 構成

```
webapp/
  index.html              画面
  manifest.webmanifest    PWA マニフェスト
  sw.js                   Service Worker（オフラインキャッシュ）
  css/app.css             スタイル（昼夜テーマ・大タッチ UI）
  js/app.js               地図・ルート・GPS・取込・オフライン処理
  lib/leaflet/            Leaflet 1.9.4（同梱・オフライン可）
  data/*.geojson          空港・ヘリ/病院・VOR・送電線
  icons/                  アプリアイコン
```

地図タイルのみオンライン取得（未キャッシュ時）。それ以外は完全ローカル動作。
